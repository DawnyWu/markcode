<nav id="workflowNavbar" class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <a id="logo" class="navbar-brand" href="/" title="" alt="">Markcode</a>
    </div>

    <div id="navbar" class="navbar-collapse collapse">

      <ul class="nav navbar-nav navbar-right">
        <li>
          <%= form_tag search_path, method: :get, class: "navbar-form", role: "search" do |f| %>
            <div class="form-group">
              <%= text_field_tag :q, params[:q], class: "form-control" %>
            </div>
          <% end %>
        <%#= form_tag '/search', method: :get, class: "navbar-form", role: "search" do %>
          <!-- <div class="form-group"> -->
            <!-- <input type="text" name="search" class="form-control" placeholder="搜索标题"> -->
          <!-- </div> -->
        <%# end %>
        </li>
        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.name %> <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <!-- <li role="separator" class="divider"></li> -->
              <li>
                <%= link_to "注销", destroy_user_session_path, method: :delete %>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to "用户登陆", new_user_session_path %></a></li>
        <% end %>
      </ul>

    </div>
  </div>
</nav>
<script type="text/javascript">
  $('.dropdown-toggle').dropdown();
  $("#q").autocomplete({
    source: "/search",
    select: function(event, ui) {
      location.href = ui.item.url;
    }
  })
</script>